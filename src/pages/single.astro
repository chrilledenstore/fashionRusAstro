---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
const { product } = Astro.props;

const selectedProduct = await fetch("https://kea-alt-del.dk/t7/api/products/1532").then((response) => response.json());
console.log(selectedProduct);
---

<Layout>
  <header>
    <Header />
  </header>

  <main class="product_view_main">
    <section class="product_view_container">
      <img src="https://kea-alt-del.dk/t7/images/webp/640/1532.webp" alt="sports trøje" />
    </section>
    <section class="purchase_box">
      <h3>{selectedProduct.productdisplayname}</h3>
      <p>{selectedProduct.articletype} | {selectedProduct.brandname}</p>
      <form>
        <label>
          Select size
          <select name="size">
            <option>S</option>
            <option>M</option>
            <option>L</option>
          </select>
        </label>
        <button>Add to basket</button>
      </form>
    </section>
  </main>

  <footer>
    <Footer />
  </footer>
</Layout>
